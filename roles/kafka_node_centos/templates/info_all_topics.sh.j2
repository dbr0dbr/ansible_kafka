#!/bin/bash
for topic in `{{ kafka_dir }}/bin/kafka-topics.sh --zookeeper localhost:2181 --list`; do
        {{ kafka_dir }}/bin/kafka-topics.sh --describe --zookeeper 127.0.01:2181 --topic $topic
done
