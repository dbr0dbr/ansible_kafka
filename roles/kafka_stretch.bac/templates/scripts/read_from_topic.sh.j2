#!/bin/bash
if [ -z $1 ]; then echo "usage: read_from_topic topic_name" ; exit 1; fi
topicname=$1
{{ kafka_dir }}/bin/kafka-console-consumer.sh --bootstrap-server localhost:9092 --topic $topicname --from-beginning
