#!/bin/bash
if [ -z $2 ]; then echo "usage: write_to_topic.sh topic_name message_text" ; exit 1; fi
echo "$2" | {{ kafka_dir }}/bin/kafka-console-producer.sh --broker-list localhost:9092 --topic $1 > /dev/null
